<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @image.title %>
</p>

<p>
  <strong>User:</strong>
  <%= User.find(@image.user_id).username %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @image.image %>
</p>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
